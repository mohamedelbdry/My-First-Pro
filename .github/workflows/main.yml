on: 
  push:
    branches:
      - master






jobs:
  build: 
    run-on: ubuntu-latest

    steps:
      - name: 'Testing Github action'
        run: pwd

      - name: 'Before'
        run: ls

       - uses: actions/chechout@v3 

       - name: 'After'
        run: ls
